version: "3.3"
services:
  postgres:
    image: "postgres:latest"
    ports:
      - "5432:5432"
    volumes:
      - postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=locutor
      - POSTGRES_PASSWORD=locutor-pass
      - POSTGRES_DB=erp-database
volumes:
  postgres:
